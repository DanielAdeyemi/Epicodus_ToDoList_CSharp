@using ToDoList.Models;

<h1>To Do List</h1>
@if (Model.Count == 0)
{
  <p>There are no items in a list.</p>
}
<ul>
  @foreach (Item item in Model)
  {
    <li>@Model.Description</li>
  }
</ul>
<a href="/items/new">Add a new item.</a>